# ============================================================
#  Joyce Photos bot — environment variables
#  Copy this to .env and fill in the REAL values.
#  NEVER commit the filled-in .env (it is gitignored).
#  Fill values by copying them out of Railway (see ENV_TRANSFER.md).
# ============================================================

# ── Telegram (from @BotFather / @userinfobot) ──
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USER_ID=
TELEGRAM_CHANNEL=

# ── GitHub (Personal Access Token with repo write) ──
GITHUB_TOKEN=
GITHUB_REPO=NBiryukov25/joyce-photos-gallery
GITHUB_BRANCH=main

# ── Data source ──
AIRTABLE_TOKEN=

# ── AI / transcription services ──
ANTHROPIC_API_KEY=
GROQ_API_KEY=
TRANSCRIBE_API_TOKEN=

# ── Sharing / hosting ──
NETLIFY_SITE_URL=
SHARE_SECRET=

# ── Platform-provided: DO NOT copy this one. ──
# Railway/Render set PORT automatically. Leave it unset.
# PORT=
